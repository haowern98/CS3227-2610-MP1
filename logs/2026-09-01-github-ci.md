# GitHub continuous integration

## Prompt summary

The student requested comprehensive GitHub CI without copying another student's repository. The
approved scope covered cross-platform Java 25 verification and documentation formatting checks.

## AI assistance

Codex reviewed the project's Gradle tasks and documentation, checked current official GitHub Action
versions, and drafted an original workflow for this repository.

## Verification

- `actionlint` 1.7.12 accepted `.github/workflows/ci.yml`.
- `markdownlint-cli2` 0.23.2 reported no issues in the configured project documentation.
- `./gradlew.bat clean test javadoc assemble` passed locally with Temurin 25.0.4.1 on Windows 11.
- The first hosted run exposed a missing executable bit on `gradlew`; the wrapper mode was corrected
  from `100644` to `100755`.
- The next hosted run passed its Windows, Linux, macOS, and documentation jobs. Gradle reported its
  existing Javadoc warnings without failing the build.
